import { render, screen } from "@testing-library/react";

describe("topicsSlice", () => {
  it("has initial state ", () => {});

  it("addTopics adds an object to state immutably", () => {});

  it("selectTopics selects the nested topics object in initial state", () => {});
});
